@model IEnumerable<BrightPathDev.Models.Article>

@{
    ViewData["Title"] = "Index";
}


<h1> Top Picks</h1>
<hr width="80%" />

<hr />
<div class="side-container">
    <div class="Create align-content-lg-end">
        <a asp-action="Create" class="create-button">Create New</a>
    </div>


</div>

<!--<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ArticleTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.desc_mini)
            </th>
            <th>0
                @Html.DisplayNameFor(model => model.desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArticleAdress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArticleLat)
            </th>
    <th>
                @Html.DisplayNameFor(model => model.ArticleLng)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ArticleContact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ArticleTitle)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.desc_mini)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.desc)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ArticleAdress)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ArticleLat)
                </td>
    <td>
                    @Html.DisplayFor(modelItem => item.ArticleLng)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ArticleContact)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ArticleId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ArticleId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ArticleId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

    -->
<br />

<div class="row">
    @foreach (var item in Model)
    {
        @if (item.Status == ContactStatus.Approved)
        {
            <div class="card col-4">
                @if (item.ImagePath != null)
                {
                    <img class="card-img-top" src="~/Image/@item.AuthorId/@item.ArticleId/@Url.Content(item.ImageName)" alt="Article image">
                }

                <div class="card-body">
                    <a asp-action="Details" asp-route-id="@item.ArticleId">
                        <h5 class="card-title">
                            @Html.DisplayFor(modelItem => item.ArticleTitle)
                        </h5>
                    </a>
                    <p class="card-text">
                        @Html.DisplayFor(modelItem => item.desc_mini)
                    </p>
                    <div>@item.LikeCount Likes ! |@item.DislikeCount Dislikes !</div>
                    <a asp-action="Details" asp-route-id="@item.ArticleId" class="btn btn-primary">View Article</a>
                </div>
            </div>
        }
    }
</div>